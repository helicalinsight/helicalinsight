# Helical Insight Docker settings
# Copy this file to .env and change only if you need to.

# Your machine's IP or hostname (used in the app base URL)
HOST_IP=localhost

# Optional — uncomment only if you need them:
# HTTPS_PORT=443
# INSTALL_CHROME=true
# POSTGRES_USER=hiuser
# POSTGRES_PASSWORD=hiuser
# POSTGRES_DB=hiee
