# CI tooling versions — bump here only (workflow reads this file).
#
# Lists: comma-separated (preferred) or quoted spaces.
#   JAVA_VERSIONS=25,26
#   NODE_VERSIONS=20,22
# *_RELEASE_VERSION is used for tag packaging (must appear in the matching list).
# Use JDKs >= server/pom.xml <jdk.version> (currently 25) or Java builds will fail.
JAVA_VERSIONS=25,26
JAVA_RELEASE_VERSION=25
JAVA_DISTRIBUTION=temurin
NODE_VERSIONS=20,24
NODE_RELEASE_VERSION=20
