# Presence of this marker file on the (test) classpath enables DriverClassCompat.
# It ships only in src/test/resources, so legacy Derby driver-class names are
# normalized ONLY during test execution and never in production.
