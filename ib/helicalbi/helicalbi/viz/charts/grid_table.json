{
  "dims_min": 1,
  "dims_max": null,
  "measures_min": 1,
  "measures_max": null,
  "instruction": "Pivot-style grid table with row/column hierarchy (GridTable / S2), not a flat row table.",
  "aliases": [
    "Grid Table",
    "GridTable",
    "S2Chart",
    "crosstab",
    "pivot grid",
    "s2 table"
  ],
  "requires_ordered": false,
  "base": "default",
  "conversion": {
    "family": "table",
    "component": "GridTable",
    "fields": {
      "dimension_column": {
        "role": "dimension",
        "index": 0,
        "optional": true
      },
      "measure_column": {
        "role": "measure",
        "index": 0,
        "optional": true
      }
    },
    "omit_when_missing": []
  },
  "instructions": [
    "GridTable = AntV S2 wrapper (NOT Ant Design Table). Keep the VF SHORT — use helperFunctions.buildGridTableReport.",
    "1) Replace dimensions/measures with real result-column keys from data/SQL (must match Object.keys(data[0])).",
    "2) First dim -> row, remaining dims -> column; measures are values. Pass formatStrings when Excel formats are provided.",
    "3) Do not inline a full report/properties blob (it truncates under max_tokens). Never use <Table />.",
    "4) Keep crosstab totals on unless the user asks to disable (showTotals: true)."
  ],
  "code": [
    "function DrawGridTable() {",
    "  const { GridTable } = components;",
    "  const { buildGridTableReport } = helperFunctions;",
    "  const minReport = buildGridTableReport({",
    "    dimensions: ['dimension_column'],",
    "    measures: ['measure_column'],",
    "    formatStrings: { measure_column: '0.00' },",
    "    showTotals: true",
    "  });",
    "  return (",
    "    <div>",
    "      <GridTable",
    "        {...report}",
    "        data={data}",
    "        fields={minReport.fields}",
    "        metadata={minReport.metadata}",
    "        marksList={minReport.marksList}",
    "        report={minReport}",
    "      />",
    "    </div>",
    "  );",
    "}"
  ]
}
