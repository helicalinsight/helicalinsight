{
  "dims_min": 1,
  "dims_max": 1,
  "measures_min": 1,
  "measures_max": 1,
  "instruction": "Part-to-whole with a useful center total/label.",
  "aliases": [
    "doughnut chart",
    "donut chart"
  ],
  "requires_ordered": false,
  "base": "default",
  "instructions": "Donut via Pie. Config: angleField, colorField, radius~0.8, innerRadius~0.6; statistic title+content (total).\nPolish: center title short; content = sum(measure); outer labels or legend (not both if clutter); element-active; appendPadding.",
  "code": "\nfunction DrawDoughnut() {\n  const { Pie } = components;\n  const measureField = \"measure_column\";\n  const config = {\n    appendPadding: 10,\n    data,\n    angleField: measureField,\n    colorField: \"dimension_column\",\n    radius: 0.8,\n    innerRadius: 0.6,\n    statistic: {\n      title: {\n        style: { color: \"#000\", fontSize: 14 },\n        formatter: () => \"title_text\"\n      },\n      content: {\n        style: { fontSize: 14 },\n        content: String(\n          data.reduce((sum, row) => sum + (row[measureField] || 0), 0)\n        )\n      }\n    },\n    interactions: [{ type: \"element-active\" }]\n  };\n  return <div><Pie {...config} /></div>;\n}\n"
}
