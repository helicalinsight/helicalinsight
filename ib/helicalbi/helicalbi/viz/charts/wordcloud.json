{
  "dims_min": 1,
  "dims_max": 1,
  "measures_min": 0,
  "measures_max": 1,
  "instruction": "Categorical labels or free-text where relative frequency/prominence matters (modes, mediums, reasons, tags, 'all X'). Prefer over table for list-all category questions.",
  "aliases": [
    "word cloud",
    "tag cloud",
    "wordcloud chart"
  ],
  "requires_ordered": false,
  "base": "default",
  "conversion": {
    "family": "other",
    "component": "WordCloud",
    "fields": {
      "dimension_column": {
        "role": "dimension",
        "index": 0
      }
    },
    "omit_when_missing": []
  },
  "settings": {
    "dimensions": {
      "names": "provide dimension names as JSON array, min 1 max 1"
    },
    "measures": [
      "optional numeric weight/count measure as a JSON array, min 0 max 1 (omit or [] when non-numeric; weight then uses word frequency)"
    ],
    "labelsX": "Label for x field",
    "labelsY": "Label for y field",
    "color": {
      "mode": "optional",
      "optional": true,
      "description": "Solid hex string or palette array of hex colors"
    }
  },
  "instructions": [
    "Fill ChartSettings only (JSON). Do NOT invent JS/JSX or component props.",
    "dimensions.names and measures must be real result-column names from data_types / sample_row (JSON arrays).",
    "dimensions.names[0] = word/text field.",
    "measures[0] is optional and MUST be numeric to bind as weightField; if missing or non-numeric, ignore that measure for weight and size words by frequency.",
    "Set labelsX, labelsY to user-friendly axis titles (aliases OK; need not equal column names).",
    "color: solid hex string or palette array when the template asks; omit if unused.",
    "Do NOT return measure_formats here — formats are a separate step."
  ],
  "code": [
    "function DrawWordcloud() {",
    "  const { WordCloud } = components;",
    "  const setting = ${setting};",
    "  const measure_formats = ${format};",
    "  const wordField = setting.dimensions.names[0];",
    "  const measureField = setting.measures && setting.measures[0];",
    "  const hasNumericMeasure = !!(measureField && (data || []).some((row) => {",
    "    const value = row?.[measureField];",
    "    return value != null && value !== '' && Number.isFinite(Number(value));",
    "  }));",
    "  let chartData = data;",
    "  let weightField;",
    "  if (hasNumericMeasure) {",
    "    weightField = measureField;",
    "  } else {",
    "    // Non-numeric / missing measure: ignore weightField binding; size by frequency.",
    "    const counts = {};",
    "    (data || []).forEach((row) => {",
    "      const key = row[wordField];",
    "      if (key == null || key === '') return;",
    "      counts[key] = (counts[key] || 0) + 1;",
    "    });",
    "    weightField = '__weight';",
    "    chartData = Object.keys(counts).map((name) => ({",
    "      [wordField]: name,",
    "      [weightField]: counts[name]",
    "    }));",
    "  }",
    "  const config = {",
    "    data: chartData,",
    "    wordField,",
    "    weightField,",
    "    ...(setting.color ? { color: setting.color } : {}),",
    "  };",
    "  return <div><WordCloud {...config} /></div>;",
    "}"
  ]
}
