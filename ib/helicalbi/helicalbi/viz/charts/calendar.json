{
  "dims_min": 1,
  "dims_max": 1,
  "measures_min": 0,
  "measures_max": 1,
  "instruction": "Values/events must be arranged by calendar date.",
  "aliases": [
    "calendar heatmap",
    "calendar chart",
    "date heatmap"
  ],
  "requires_ordered": true,
  "base": "default",
  "conversion": {
    "family": "heatmap",
    "component": "Heatmap",
    "fields": {},
    "omit_when_missing": []
  },
  "settings": {
    "dimensions": {
      "names": "provide dimension names as JSON array, min 2 max 2"
    },
    "measures": [
      "provide measure names separated by comma, min 0 max 1"
    ],
    "labelsX": "Label for x field",
    "labelsY": "Label for y field",
    "color": {
      "mode": "palette",
      "optional": false,
      "description": "Sequential hex palette for the heat scale"
    }
  },
  "instructions": [
    "Fill ChartSettings only (JSON). Do NOT invent JS/JSX or component props.",
    "dimensions.names and measures must be real result-column names from data_types / sample_row (JSON arrays).",
    "dimensions.names = [weekday-or-x, month-or-y] (2 entries); optional measures[0] for intensity.",
    "Set labelsX, labelsY to user-friendly axis titles (aliases OK; need not equal column names).",
    "color: solid hex string or palette array when the template asks; omit if unused.",
    "Do NOT return measure_formats here — formats are a separate step."
  ],
  "code": [
    "function DrawCalendar() {",
    "  const { Heatmap } = components;",
    "  const setting = ${setting};",
    "  const measure_formats = ${format};",
    "  const xDim = setting.dimensions.names[0];",
    "  const yDim = setting.dimensions.names[1];",
    "  const config = {",
    "    data,",
    "    xField: xDim,",
    "    yField: yDim,",
    "    colorField: setting.measures[0],",
    "    color: setting.color || ['#BAE7FF', '#1890FF', '#0050B3'],",
    "    meta: {",
    "      [xDim]: { type: 'cat' },",
    "      [yDim]: { type: 'cat' }",
    "    },",
    "    legend: { position: 'bottom' }",
    "  };",
    "  return <div><Heatmap {...config} /></div>;",
    "}"
  ]
}
