{
  "dims_min": 1,
  "dims_max": 1,
  "measures_min": 1,
  "measures_max": 1,
  "instruction": "Compact discrete comparison without full axes.",
  "aliases": [
    "sparkline column",
    "mini column",
    "tiny column chart"
  ],
  "requires_ordered": true,
  "base": "default",
  "conversion": {
    "family": "tiny",
    "component": "TinyColumn",
    "fields": {
      "dimension_column": {
        "role": "dimension",
        "index": 0
      },
      "measure_column": {
        "role": "measure",
        "index": 0
      }
    },
    "omit_when_missing": []
  },
  "settings": {
    "dimensions": {
      "name": "provide the dimension name min 1 and max 1"
    },
    "measures": [
      "provide measure names separated by comma, min 1 max 1"
    ],
    "color": {
      "mode": "optional",
      "optional": true,
      "description": "Solid hex string or palette array of hex colors"
    },
    "title": "Chart title"
  },
  "instructions": [
    "TinyColumn sparkline. Config: xField, yField; autoFit.",
    "Polish: no axes/legend; compact bars; discrete comparison only."
  ],
  "code": [
    "function DrawTinyColumn() {",
    "  const { TinyColumn } = components;",
    "  const setting = ${setting};",
    "  const measure_formats = ${format};",
    "  const measureField = (setting.measures && setting.measures[0]) || '';",
    "  const config = {",
    "    data,",
    "    xField: setting.dimensions.name,",
    "    yField: setting.measures[0],",
    "    ...(setting.color ? { color: setting.color } : {}),",
    "    meta: {",
    "      [measureField]: { alias: setting.title || measureField }",
    "    }",
    "  };",
    "  return <div><TinyColumn {...config} /></div>;",
    "}"
  ]
}
