{
  "dims_min": 1,
  "dims_max": 1,
  "measures_min": 1,
  "measures_max": null,
  "instruction": "Trend over time or another continuous ordered dimension.",
  "aliases": [
    "line chart",
    "trend line",
    "line plot"
  ],
  "requires_ordered": true,
  "base": "default",
  "conversion": {
    "family": "cartesian",
    "component": "Line",
    "fields": {
      "dimension_column": {
        "role": "dimension",
        "index": 0
      },
      "measure_column": {
        "role": "measure",
        "index": 0
      }
    },
    "omit_when_missing": []
  },
  "instructions": [
    "Line. Config: xField=ordered dim, yField=measure; seriesField if multi; smooth optional.",
    "Polish: autoFit; point size 3-5 (circle/diamond) sparingly; axis titles; legend top if series; slider if many points; interactions; avoid heavy area fill unless asked."
  ],
  "code": [
    "function DrawLine() {",
    "  const { Line } = components;",
    "  // Fill from Excel format strings for matching measure names only; omit unused keys.",
    "  const measureFormats = {",
    "    measure_column: '0.00'",
    "  };",
    "  const formatMeasure = (v, field = 'measure_column') => {",
    "    const fmt = measureFormats[field];",
    "    const raw = (v != null && typeof v === 'object') ? (v[field] ?? v.value ?? v) : v;",
    "    if (!fmt || raw == null || raw === '') return raw;",
    "    const num = typeof raw === 'number' ? raw : Number(raw);",
    "    if (typeof num !== 'number' || !Number.isFinite(num)) return raw;",
    "    if (fmt === '0.00' || fmt === '0.0#' || /^0\\.0+$/.test(fmt)) {",
    "      return num.toFixed((fmt.split('.')[1] || '').length);",
    "    }",
    "    if (fmt.includes('#') || fmt.includes(',')) {",
    "      const decimals = (fmt.split('.')[1] || '').replace(/[^0]/g, '').length;",
    "      return num.toLocaleString(undefined, {",
    "        minimumFractionDigits: decimals,",
    "        maximumFractionDigits: decimals || 0",
    "      });",
    "    }",
    "    return raw;",
    "  };",
    "  const config = {",
    "    data,",
    "    xField: 'dimension_column',",
    "    yField: 'measure_column',",
    "    xAxis: {",
    "      title: {",
    "        text: 'dimension_column'",
    "      }",
    "    },",
    "    yAxis: {",
    "      title: {",
    "        text: 'measure_column'",
    "      },",
    "      label: {",
    "        formatter: (v) => formatMeasure(v, 'measure_column')",
    "      }",
    "    },",
    "    point: {",
    "      size: 5,",
    "      shape: 'diamond'",
    "    },",
    "    meta: {",
    "      measure_column: { alias: 'measure_column', formatter: (v) => formatMeasure(v, 'measure_column') }",
    "    }",
    "  };",
    "  return <div><Line {...config} /></div>;",
    "}"
  ]
}
