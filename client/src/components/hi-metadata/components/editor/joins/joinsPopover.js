import React from 'react';

function JoinsPopover() {
    return <div>
        <p>Drag and drop the columns to create joins.</p>
        <p>Order of the joins may affects the query generation.</p>
        <p>To change order of joins you can drag the joins up or down.</p>
    </div>
}

export default JoinsPopover;
