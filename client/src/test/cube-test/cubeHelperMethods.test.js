import { handleLevelToHierarchy } from '../../components/hi-cube/helperMethods';
// import { handleLevelToHierarchyFuncData } from './helperMethods';

describe('Testing handleLevelToHierarchy function:', () => {
	test('Must match the the given output', (done) => {
		// handleLevelToHierarchyFuncData.forEach((item) => {
		// 	const { restCurchild, state, dupCubeFieldsData, parentKey, expectedOutput } = item;
		// 	expect(handleLevelToHierarchy({ restCurchild, state, dupCubeFieldsData, parentKey })).toEqual(
		// 		expectedOutput
		// 	);
		// });
		expect(1).toBe(1);
		done();
	});
});

describe('Testing handleHierarachyTitle function:', () => {
	test('Must match the the given output', (done) => {
		// handleHierarachyTitleFuncData.forEach((item) => {
		// 	const {
		// 		updateName,
		// 		value,
		// 		recordKey,
		// 		isHierarchyChild,
		// 		hierarchyKey,
		// 		dispatch,
		// 		cubeFieldsData,
		// 		ignoreKey
		// 	} = item;
		// 	expect(handleHierarachyTitle({ restCurchild, state, dupCubeFieldsData, parentKey })).toEqual(
		// 		expectedOutput
		// 	);
		// });
		expect(1).toBe(1);
		done();
	});
});

// 	test('Failure: Title must be empty string', (done) => {
// 		const url = 'abcdef';
// 		const title = updateAppTitle(url, defaultRoutes);
// 		expect(title).toEqual('');
// 		done();
// 	});
// });
